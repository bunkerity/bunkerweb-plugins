version: '3'

services:
  clamav-api:
    image: bunkerweb-clamav:tests
    ports:
      - 8000:8000
    environment:
      - CLAMAV_HOST=clamav

  clamav:
    image: clamav/clamav:0.104
    volumes:
      - ./clamav-data:/var/lib/clamav
